{
  "presets": [
    ["@babel/preset-env", { "targets": { "node": "current" } }], // Compile modern JavaScript for current Node.js version
    ["@babel/preset-react", { "runtime": "automatic" }] // Enable React JSX transformation
  ]
}
