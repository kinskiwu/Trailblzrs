import React from 'react';

const ExportButton = () => {
  return (
    <div>
      <button className='export-button'>📊 Export to Google Sheet</button>
    </div>
  );
};

export default ExportButton;
